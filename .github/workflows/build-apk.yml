name: Build APK (Capacitor)

on:
  workflow_dispatch:
  push:
    branches: [ "master", "main" ]

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v

cd ~/dr-ahlam-prices-offline
mkdir -p .github/workflows

cat > .github/workflows/build-apk.yml <<'YML'
name: Build APK (Capacitor)

on:
  workflow_dispatch:
  push:
    branches: [ "master", "main" ]

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install npm deps
        run: |
          npm ci || npm install
          # ensure capacitor packages exist (no-save)
          npm i --no-save @capacitor/core @capacitor/cli @capacitor/android

      - name: Ensure Android platform + sync
        run: |
          npx --yes -p @capacitor/cli cap --version
          if [ ! -d android ]; then
            npx --yes -p @capacitor/cli cap add android
          fi
          npx --yes -p @capacitor/cli cap sync android

      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew --no-daemon assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
